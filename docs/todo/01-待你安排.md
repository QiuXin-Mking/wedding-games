# 待你安排的事

> 更新：2026-09-22 ｜ 婚礼：**2026-10-06（周二）**
> 这些是我做不了的 —— 需要真人、真手机、真场地，或者需要新人提供内容。
> 按**截止日**排序，不是按重要性。

---

## 🔴 9/28 前必须完成（国庆假期从 10/1 开始，之后很难补救）

### 1. 婚礼场地 WiFi 实测 —— 这条最紧急

**做什么**：带一部手机到婚礼场地，**连场地 WiFi**（不是 4G），扫码打开
`http://119.29.186.63:8888/`，确认能进入并看到「进入」按钮。

**为什么急**：8888 是非标准端口，部分酒店和企业网络会拦截。
**这条不通就得换端口重新部署 + 重印所有二维码桌卡** —— 拖到彩排就来不及印了。

**怎么算通过**：页面能打开、能点「进入」、能拿到昵称。

> 对应 AC-37 / RISK-16

### 2. 三网实测

**做什么**：移动、联通、电信各一部手机，**用自己的流量**（不是同一个 WiFi），
分别扫码打开。三网都要试 —— 都连同一个 WiFi 测的是同一条链路，等于没测。

> 对应 AC-34

### 3. 微信内实测

**做什么**：用**微信扫一扫**打开（不是浏览器），走完整流程。

会看到「非微信官方网页」的提示条，**这是预期的**，点继续访问即可。
需要确认的是这个提示条能点过去、不会把人挡在外面。

> 对应 AC-33 / C4

### 4. 二维码桌卡印刷 + 实扫

**做什么**：`public/qr.png`（480×480）拿去印桌卡，印出来后**用真手机实扫一次**。

屏幕上能扫 ≠ 印出来能扫 —— 纸张反光、油墨扩散、尺寸缩小都会影响。
建议二维码印刷尺寸不小于 **4cm × 4cm**。

**桌卡上建议写的字**（演练中长辈反映光一个黑方块不知道要干嘛）：
> 微信扫一扫，点一下「进入」就行，不用打字，也不用填手机号

> 对应 FR-9.3 / AC-33

---

## 🔴 10/1 前

### 5. 婚礼题库 **16 道**（不是 13 道）

**做什么**：新人提供 **13 道正题 + 3 道备用题**。

备用题是给主持人救场用的：某道题念错了或有歧义，就换一道备用题重来
（不能重答原题 —— 答案刚在大屏上公布过，重答等于集体送分）。

**格式**（照 `questions/test.json` 填，`answer` 是正确选项的下标，从 0 开始）：
```json
[
  { "text": "新郎新娘是在哪里认识的？",
    "options": ["大学社团", "同事介绍", "旅行途中", "相亲"], "answer": 1 },
  { "text": "（第 14~16 条是备用题，加 spare 标记）",
    "options": ["甲", "乙"], "answer": 0, "spare": true }
]
```

**约束**：
- 正题必须**恰好 13 道**，备用题**至少 1 道**（建议 3 道）
- 每题至少 2 个选项；判断题就写「对 / 错」两个选项
- **纯文字，不能有图片**
- 题干别太长 —— 20 秒要读完题干和四个选项

填好存成 `questions/wedding.json`，服务端启动时会自动校验，
**格式不对会直接启动失败并指出是第几条的什么问题**，不会带着坏数据开场。

> 对应 RISK-5 / AC-16 / AC-17

---

## 彩排前（建议 10/1~10/2）

### 6. 找一位 **50 岁以上**的人独立过目昵称池

**做什么**：先生成一份当前名单，

```bash
node tools/print-names.js --md 昵称池快照.md
```

把它给一位 50 岁以上的长辈看，**不要在旁边解释**，让他/她自己指出哪些不舒服。
看完把快照删掉 —— **不要提交进仓库**。

> 仓库里原先存过一份手工维护的 `昵称池-待审核.md`，2026-09-22 发现它已经和
> `nicknames.json` 对不上（第 213 号表里写「沉稳的海龟」，实际发的是
> 「香甜的八宝饭」）。审核一份不是真正会发出去的名单，比不审核更糟 ——
> 审完会以为放心了。所以副本删掉，要看就现生成。

**为什么必须是长辈**：上一版昵称池栽过一次 —— 里面有
`贪吃的仓鼠`、`爱赖床的熊猫`、`爱吃席的柯基`，每个词单看都可爱，
但挂在一位 56 岁女性头上再投到全场大屏，就成了当众说她又馋又懒。
而「吃席」在部分地区指办白事。

**你我都不算数** —— 审的人和被安名字的人语境不同，这道关我们俩谁都替代不了。

> 对应 AC-22

### 7. 纸质题卡（含答案）

**做什么**：把 16 道题连同正确答案打印一份，交给主持人。

**为什么**：应急预案里「服务器整个连不上 → 转口头抢答」这条，
如果主持人手上没有题目就**根本执行不了** —— 题目在服务器上。

> 对应 AC-59

### 8. 指定记分员 + 准备纸笔

**做什么**：找一位伴娘/伴郎当记分员，准备纸和笔，放在主持人够得着的地方。

同样是为了「服务器挂了转口头抢答」—— 没人记分，这条预案也是空的。

> 对应 AC-60

### 9. 与主持人交底，并让他**独立**跑通一遍

**做什么**：
- 把控制台交给主持人，讲一遍
- 然后**你不说话**，让他自己从「开始游戏」跑到「公布排行」
- 中途让他试一次「跳过本题」和「换一道备用题」

**验收线是「他能独立跑通」**，不是「他听懂了」。
文档写得再好，主持人现场不敢点按钮就等于没做。

**要特别跟他说清的两件事**：
1. **先念完题再点「下一题」** —— 点下去倒计时立刻开始走
2. 喊完「大家扫码」之后，**要有约 8 秒的垫场话** ——
   400 人同时扫码，页面加载需要这么久（1 秒内会先出「正在进入」，不会白屏）

> 对应 AC-36 / AC-39

---

## 婚礼当天（10/6）

### 10. 开场前的三件事

1. **切正式题库**：`QUIZ_BANK=wedding` 并重启服务
   （切完确认大屏左上角角标显示「**正式题库**」，不是「测试题库」）
2. **三端强制刷新**（Ctrl+F5 / 长按刷新）——
   静态资源缓存一小时，彩排后若改过代码，不刷新会用到旧版本
3. **大屏点「全屏」** —— 不全屏的话地址栏里的口令会投给全场

### 11. 结束后

把服务器 `/opt/wedding-quiz/data/` 下的事件日志拷走留存，
并让主持人在控制台点一次「导出明细 CSV」（Excel 打开，中文不会乱码）。

---

## 已经不用管的

- ~~服务器~~ 已就位：`119.29.186.63`，8888 已放行，服务已装好并开机自启
- ~~Node 环境~~ 已装 v20.20.2
- ~~二维码~~ 已生成并验证（`public/qr.png`）
- ~~GitHub 仓库~~ 已建
