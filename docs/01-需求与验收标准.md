# 01 · 需求与验收标准

> 婚礼互动答题小游戏 H5 ｜ 婚礼日期 2026-10-06
> 本文件自包含：**只写最终结论与可检验的标准**，不含推导过程。

---

## 1. 产品定义

婚礼现场「大屏 + 宾客手机」实时答题：宾客扫码入场 → 系统分配昵称 → 主持人播报题目 →
大屏与手机同步出题并倒计时 → 宾客作答 → 实时计分 → 大屏排行榜。共 13 题。

**三端**

| 端 | 路由 | 使用者 |
|----|------|--------|
| 手机端 | `/` | 宾客（二维码指向此地址） |
| 大屏端 | `/screen?key=<口令>` | 婚礼大屏，1920×1080 横屏 |
| 主持人端 | `/host?key=<口令>` | 主持人手机 / 平板 |

**传输协议**：WebSocket（`ws://`，与 HTTP 同端口）。选型论证见 §2.3。

**运行形态**：腾讯云 **OpenCloudOS 9.6**（RHEL 系，包管理 `dnf`），2 核 2G，37G 可用磁盘。
公网 `119.29.186.63`，SSH 免密（root）。单进程、单端口 **8888**，`http` 明文 + `ws://`，
不备案、无域名、无 HTTPS。本机实测 RTT **平均 12ms**（最低 7ms），TCP 握手 3~24ms。
**SELinux 已禁用、firewalld 未启用**，仅云安全组生效；**8888 已用真实 HTTP 响应验证放行**
（以未放行的 9999 作对照组）。

---

## 2. 技术硬约束

| ID | 约束 |
|----|------|
| C1 | 服务端监听高位端口 **8888**，不得用 80/443（未备案的 80 端口被云厂商拦截）。8888 是常见的 HTTP 备用端口 |
| C2 | 页面运行在 http 明文下，**禁止依赖任何需要 HTTPS 安全上下文的浏览器能力**（摄像头、剪贴板 API、Service Worker、Web Push、地理位置） |
| C3 | WebSocket 使用 `ws://`，与 HTTP **同端口**（同进程协议升级） |
| C4 | 微信内打开会出现「非微信官方网页」提示条，属预期行为，页面与现场话术均须承接 |
| C5 | 服务器只开放 **8888** 一个端口。实测 firewalld 未启用、SELinux 已禁用，**只需云安全组**（已放行）。若后续启用 firewalld，须同步放行 |
| C6 | **依赖极简**，见 §2.1 |
| **C7** | **仓库为 public**（https://github.com/QiuXin-Mking/wedding-games）。新人的题目属新人自己的信息，已决定**随仓库公开提交**；但 `data/`（事件日志，含 400 位宾客的昵称与成绩）是**他人的数据、未经其同意**，必须始终在 `.gitignore` 中，绝不入库 |

### 2.3 带宽预算（**3 Mbps = 366 KB/s**，用户提供）

#### 传输协议选型（决策 D13）

| 方案 | 持续带宽占用 | 满足 AC-02（1 秒内同步）？ |
|------|-------------|--------------------------|
| HTTP 轮询 500ms | 800 req/s × 350B = **2.24 Mbps，占 75%** | ✅ 但与首屏加载抢带宽 |
| HTTP 轮询 1s | 400 req/s × 350B = **1.12 Mbps，占 37%** | ⚠️ 最坏 1s + RTT，**踩线** |
| **WebSocket** | 单次广播 400×250B = 100 KB，**0.27 秒发完**；持续 ≈ **1%** | ✅ 有余量 |

**结论：采用 WebSocket。** 理由不是「实时游戏惯例」，而是 3 Mbps 这个具体数字把轮询挤掉了 ——
轮询要么吃掉四分之三带宽，要么延迟正好卡在验收线上。
全场 13 题总推送量约 3.6 MB，摊在 15 分钟里平均 4 KB/s。

> 轮询本可以消灭一整类问题（无连接态、无重连、无退避，且「在线人数」这个
> 会导致提前结算出错的概念根本不存在）。是带宽否决了它，不是它不好。
> 因此 WebSocket 方案**必须额外处理**这些连接态问题（见 §5 缺陷 2/3/4/7/8）。


#### 为什么是 WebSocket 而不是裸 TCP

**WebSocket 本身就是 TCP 长连接** —— TCP 之上一层很薄的分帧，经一次 HTTP Upgrade 建立。

| 项 | 开销 |
|----|------|
| 帧头 | **2 字节**（服务端→客户端）/ 6 字节（客户端→服务端，含掩码） |
| 250 字节消息 | **0.8% ~ 2.4%** |
| 握手 | 每客户端一次约 400 字节 |

裸 TCP 省不下来 —— 自己做分帧同样要 2~4 字节长度前缀。
且有一条决定性事实：**浏览器没有裸 TCP 的 API**，H5 只能用
HTTP / WebSocket / WebRTC / WebTransport。WebRTC DataChannel 需要
STUN/TURN + 信令 + DTLS，复杂度爆炸且违反 C6。

**协议不是字节大头，首屏静态资源才是。**

#### 入场峰值：集中扫码（已确认为真·峰值）

主持人喊完口令后 400 人几秒内同时扫码，**不可能提前打散**。TCP 公平分配下每人约 900 B/s。

| 项 | 量 |
|----|----|
| 400 × 单文件 HTML（10 KB gzip） | 4.0 MB |
| 400 × WebSocket 握手 | 0.16 MB |
| 400 × 首次 `snapshot` | 0.2 MB |
| **合计 ÷ 366 KB/s** | **≈ 12 秒** |

注意这是「**所有人一起等 12 秒**」，不是有快有慢 —— 公平分配下大家同时完成。

**对策（全部为强制要求）**：

| ID | 要求 | 效果 |
|----|------|------|
| **P-1** | **单文件内联**：HTML + CSS + JS 全部内联进一个 `.html`，**只发 1 个请求** | 省字节，更省 400×4=1600 次请求往返 |
| **P-2** | **首屏 ≤ 10 KB（gzip 后）**，见 AC-31 | 线性决定峰值时长 |
| **P-3** | **HTML 前 600 字节内必须含 `<style>` 与一句可见文字** | 浏览器流式渲染，**1 秒出「正在进入」**，12 秒完全可交互 |
| **P-4** | **内联 data URI favicon** | 否则浏览器自动多发 400 次 `/favicon.ico` |
| **P-5** | **静态资源预压缩**为 `.gz` 落盘，不做运行时压缩 | 省 CPU，峰值期 CPU 要留给 WS |
| **P-6** | `Cache-Control: max-age=3600` | 刷新与重连不重复下载 |
| **P-7** | **昵称池与题库一律不下发客户端** | 昵称池 400 条无必要；题库含答案，下发即作弊 |

> P-3 同时治掉演练问题 A-04 / B-04：白屏让宾客以为没扫上而反复重扫，
> 而重扫会让本已紧张的带宽进一步恶化 —— 这是会自我放大的故障。

#### 连接层限制（实测并已修复 2026-09-22）

```
somaxconn 4096 ✅    tcp_max_syn_backlog 2048 ✅    tcp_syncookies 1 ✅    内核 6.6 ✅
```

**fd 上限（已查清并修复）**：

| 路径 | 修复前 | 修复后 |
|------|-------|-------|
| systemd 服务 | **524288**（发行版默认，本就够用） | 不变；unit 仍显式写死不依赖默认 |
| SSH 交互式 shell | **1024** | **65535**（已写 `/etc/security/limits.d/99-wedding-quiz.conf`，新会话已验证） |

> 交互式 shell 那条路径是真会中招的场景：**婚礼当天有人 SSH 上去手动 `node server.js`**
> 排查或重启，继承的就是 1024。应急预案里正有这一步。

| ID | 要求 |
|----|------|
| **P-8** | systemd unit 显式设 `LimitNOFILE=65535`，不依赖发行版默认 |
| **P-9** | Node `server.listen()` 显式指定 backlog ≥ 1024（默认 511，低于内核 4096） |

#### 入场等待的交互设计（P-10 ~ P-13）

峰值期全场需等约 12 秒。**转圈动画不传达进度，只传达「还在等」**，12 秒的 spinner
与卡死无异。以下为强制要求：

| ID | 要求 | 说明 |
|----|------|------|
| **P-10** | **真实进度条**：在 HTML 文件每约 20% 处埋一个内联 `<script>` 上报进度 | 浏览器流式解析到哪就报到哪，**是真进度不是假动画**。成本约 200 字节。**禁止假进度条** —— 卡在 90% 比白屏更令人烦躁 |
| **P-11** | **分阶段文案**：`正在进入…` → `正在为你取名字…` → **昵称揭晓** | 让 12 秒有推进感 |
| **P-12** | **昵称揭晓做成一次小小的揭晓动画** | 把等待变成期待：12 秒的尽头有个属于自己的东西被揭晓。同时强化记忆（演练 A-03：宾客三秒就忘了自己的昵称） |
| **P-13** | 动画**纯 CSS、无图片**，且**只动 `transform` / `opacity`** | 图片会产生额外请求（违反 P-1/P-4）；进度条须用 `scaleX()` 而非 `width` —— 400 台老手机同时跑动画，动布局属性会掉帧 |

#### 入场首屏（历史估算，已被上方峰值预算取代）

```
400 人 × 首屏大小 ÷ 366 KB/s
  100 KB → 109 秒      30 KB → 33 秒      20 KB → 22 秒
```

**若主持人喊「大家现在一起扫码」，最后一位宾客会白屏近两分钟。** 两条对策：

1. **首屏压到 30 KB 以内**（AC-31）。零框架下现实可达；若用 React 则首屏 150 KB+，
   400 人需 164 秒 —— C6 这条约束当初是为了少出错，实际也是带宽的救命稻草。
2. **话术禁止集中扫码**：桌卡在宾客入座时就已摆好，让其陆续扫入。
   主持人**不得**喊「现在大家一起扫」（写入 FR-9.4 操作手册）。

### 2.1 C6 依赖预算

| 层 | 允许 | 禁止 |
|----|------|------|
| 运行时依赖 | **最多 1 个**：`ws` | Express / Koa / Fastify / NestJS / Socket.IO |
| 前端 | 零框架、零构建，原生 HTML + CSS + ES Module | React / Vue / Svelte / Vite / webpack / Tailwind / UI 组件库 |
| 语言 | 纯 JavaScript，类型用 JSDoc | TypeScript（引入构建步骤） |
| 测试 | Node 内置 `node:test` | Jest / Vitest / Mocha |
| 数据 | 无数据库：内存 + JSONL 事件日志 | MySQL / PostgreSQL / SQLite / Redis / ORM |
| 二维码 | 离线一次性生成 PNG，产物入库 | 运行时二维码库 |
| CSV 导出 | 手写字符串拼接 | csv 库 |
| 部署 | `node server.js` + systemd unit | Docker / Nginx / PM2 |

**C6.1** HTTP 静态托管用 Node 内置 `http` 手写。
**C6.2** 三个页面各自独立 `.html`，`<script type="module">` 直接引入，无构建产物。
**C6.3** `npm install` 后 `node_modules` 顶层只有 `ws`。
**C6.4** 新增任何依赖前，必须先确认无法用标准库在 100 行内解决。

---

## 3. 昵称规范（已定稿）

宾客**不可自填昵称**，由服务端从一个 **400 条人工定稿的常量池**按序发放。
发放顺序在生成期定死，**运行时零随机**：第 N 位入场的宾客拿第 N 条。

**结构**：每条昵称 = `前缀词`(2~4 字，含「的」，可复用) + `核心词`(2~3 字，全池唯一)。

### 3.1 七条硬约束

| ID | 约束 | 当前池实测 |
|----|------|-----------|
| U1 | 400 条整条两两不同 | ✅ |
| **U2** | **核心词末二字两两不同**（零复用） | ✅ 0 重复 |
| **U3** | **同一首字的条目 ≤ 12 条** | ✅ 最多 6 条，212 个不同首字 |
| U4 | 长度统一 **4~6 汉字**，纯汉字（无英文 / 数字 / 符号 / 表情） | ✅ 5 字 211 / 6 字 189 |
| **U5** | **禁用词零命中**（见 §3.2） | ✅ 0 命中 |
| U6 | 核心词不得同音；**且须排除与池外常见词的歧义** | ✅ |
| U7 | **核心词必须具象**：判定法是「能不能画出来」 | ✅ |

**U7 判定**：`柯基`、`汤圆`、`灯笼`、`月牙` 能画 → 合格；
`立春`（时间概念）、`好运`（抽象祝愿）、`晴天`（天气状态）画不出来 → 不合格。

**末字封顶**：同一**末字**不超过 4 条。用于治理 U2/U6 抓不到的「听感同质」
（如 `星光/月光/极光/阳光/晨光` 末二字均不同也不同音，但听起来是一串）。

### 3.2 U5 禁用词表

判定标准：**这话能不能当众对一位 56 岁的长辈说。**

| 类别 | 禁用 |
|------|------|
| 食量身材 | 吃、馋、贪、胖、肥、圆滚、肉、膘、口福、肚皮 |
| 懒惰迟钝 | 懒、赖床、睡、打盹、发呆、呆、慢、拖、笨、迟 |
| 外貌年龄 | 丑、秃、矮、短、老、皱 |
| 排序羞辱 | 最后、倒数、垫底 |
| 婚礼忌讳 | 吃席、白、孝、离、散、分、单、空、断、落、梨（谐音「离」）、破、龟、驴 |

**豁免机制**：禁用词按**语义**判定。动物学名中的字若不构成对人的评价，
可逐条进白名单并写明理由。当前白名单仅 `老虎`（「老」是虎的固定名，无年龄义）。

### 3.3 分配与展示

- 服务端维护一个整数游标，宾客首次连接即分配 `池[cursor]` 并 `cursor += 1`。
- 入场页**无任何输入框**，只有一个「进入」按钮。
- 游标与已分配昵称写入事件日志；崩溃重放后完整恢复，**不得把同一昵称分给第二个人**。
- 超过 400 人触发兜底命名（`池[cursor % 400] + 空格 + 序号`）并告警，不崩溃。
- 手机端**所有状态下顶部常驻显示自己的昵称**，不得截断，字号不小于题干。
- 主持人可对指定宾客「发放下一个昵称」，**不可自定义输入**（不得成为绕过机制的后门）。

### 3.4 产物

| 文件 | 说明 |
|------|------|
| `nicknames.json` | 400 条，数组顺序即发放顺序 |
| `昵称池-待审核.md` | 带编号审核清单 |
| `wip/core-*.json` | 545 条候选，替换时从中取备选 |
| `tools/check-nicknames.js` | U1~U6 机器校验，**不通过禁止入库**；服务端启动时复用同一套逻辑 |

---

## 4. 游戏规则

### 4.1 题库

- **13 道正题 + 3 道备用题**，JSON 配置，字段：题号、题干、选项数组、正确选项下标、`spare`(bool)。**纯文字，无图片**。
  备用题供 `host:back` 换题使用（§4.4），不参与正常出题顺序。
- 统一为单选；判断题用 2 个选项「对 / 错」表达。
- 选项顺序**不随机打乱**，保证主持人口播的 A/B/C/D 与两端一致。
- **双题库**：`questions/test.json`（**默认加载**）与 `questions/wedding.json`（真实婚礼题）。
  **两者均随仓库提交**（C7：仓库公开，新人题目已决定公开）。
- 切换由环境变量 `QUIZ_BANK=test|wedding` 决定，不改代码、不重新构建。
- 指定 `wedding` 而文件缺失时，**启动失败并报明确错误，绝不静默回退**。
- 启动即校验：**正题 = 13 道、备用题 ≥ 1 道**、每题选项 ≥ 2、正确下标合法、题干与选项非空，
  任一不满足即启动失败。
- 大屏与主持人控制台须显示当前题库角标（`测试题库` / `正式题库`）。

### 4.2 计分

```
答对：gained = 100 + floor(max(remainMs, 0) / 1000) × 10
答错：0（不扣分）
超时：0（数据中区分 timeout / wrong / skipped）
remainMs = 服务端截止时间戳 − 服务端收到答案的时刻
```

- 单题最高 **300** 分（20 秒题即答即对），13 题理论满分 **3900** 分。
- 得分**只在服务端计算**，不采信客户端上报的任何时间戳。
- **倒计时结束前允许改答案**，按**最后一次**提交计分：`remainMs` 取最后一次提交到达服务端的时刻。
  改答案会因剩余时间变少而掉分，这是预期行为。事件日志记录每一次提交，重放时取最后一条。
- 排行榜：总分降序 → 答对题累计耗时升序 → 入场序号升序。名次唯一，不出现并列。
- `skipped` 的题不计入累计耗时。

### 4.3 倒计时

- 每题 **20 秒**，倒计时为**页面中央主视觉元素**（大屏与手机端均是）。
- **主持人先念完题、再点「下一题」**；点下的瞬间开始计时，念题时间不占用答题时间。
  状态机不设「预备态」。
- 服务端倒计时基于**单调时钟**推进，NTP 校时跳变不得导致错乱。
- 客户端连接后与服务端对时（5 次探针取最小 RTT 样本），用时钟偏移换算本地截止时刻，
  倒计时本地推进，**不逐秒依赖网络**。对时全失败时 `offset = 0` 降级。

### 4.4 状态机与主持人动作

```
IDLE --host:start--> READY --host:next--> ASKING --(超时/全员已答/手动结算)--> REVEAL
                                            ↕ pause/resume          ↓ next
                                          PAUSED                  ASKING(下一题)
                                                      host:finish --> FINAL
```

| 动作 | 合法前置态 | 计分语义 |
|------|-----------|---------|
| `host:next` | READY / REVEAL | 开下一题，`deadlineAt = now() + 20000`。**须带当前题号，服务端去重** |
| `host:extend` | ASKING | `deadlineAt += 10000` 并重播。**计分仍按原截止时刻** —— 延长期间提交 `remainMs ≤ 0`，**一律 0 分**。只给「看清题」的机会，不补分 |
| `host:earlySettle` | ASKING | **立即结算本题**，未提交者记 `timeout`、0 分。用于有人挂机时不干等 |
| `host:skip` | ASKING / REVEAL | 全员记 `skipped`、0 分、**不计入累计耗时**。大屏显示「本题作废」，**不公布答案** |
| `host:back` | REVEAL | **换一道备用题**重开（原题作废、全员 `skipped`、0 分）。不重答已公布答案的原题 |
| `host:republish` | REVEAL | 仅重播结算画面，**不触碰任何分数** |
| `host:pause` / `resume` | ASKING / PAUSED | 冻结与恢复剩余毫秒；暂停期间提交一律拒绝 |
| `host:finish` | 任意 | → FINAL，拒绝新入场 |
| `host:nextNickname` | 任意 | 给指定宾客发放 `池[cursor]`，`cursor++` |
| `host:remove` | 任意 | 移除宾客及其成绩 |

**自动结算的判定**：ASKING 态下**全部已入场宾客都已提交**即转 REVEAL。
判定分母是**已入场人数**，**不是「在线人数」** —— 长连接模型下「在线」会把掉线、
息屏、切到微信后台的宾客算作不存在，导致题目以远快于 20 秒的速度飞过。
挂机不答的情况由 `host:earlySettle` 手动兜底。

### 4.5 协议必须满足的约束

| ID | 约束 | 原因 |
|----|------|------|
| **X-1** | 自动结算分母用**已入场人数**，协议中不得引入「在线人数」概念 | 见 §4.4 |
| **X-2** | `answer` **必须有服务端回执** `answerAck{qIndex, optionIndex, accepted}`；前端**收到回执才可显示「已提交」** | 否则弱网下页面写着「已提交」而结算 0 分，宾客会认定系统吞了答案，且主持人无法自证 |
| **X-3** | `question` 载荷**必须携带该宾客在本题的提交状态**（`mySubmitted`/`myOption`） | `host:extend` 会重播 `question`，前端整体重渲染；载荷缺此字段会把「已提交」态冲掉，使 §4.4 承诺的「已提交者不受影响」在前端不成立 |
| **X-4** | 必须有面向宾客的 **`notice`** 消息类型，`extend`/`skip`/`back`/`pause`/`resume` 每个动作都要附一句宾客可读的说明 | 否则台上每按一次按钮，宾客手机就无解释地突变一次，而宾客会往「是不是我做错了」上猜 |
| **X-5** | 所有 `host:*` 动作**携带期望题号并由服务端去重** | 主持人点了没反应会本能再点；且允许多设备同开，两台同时点会连跳题 |
| **X-6** | 昵称池与题库**一律不下发客户端** | 题库含答案，下发即作弊 |

---

## 5. 三端界面要求

### 5.1 手机端（宾客）

**通用**

- 单个自包含 HTML，gzip < 10 KB，只产生 1 个请求（P-1/P-2/P-4）。
- **顶部常驻显示自己的昵称**，不截断，字号不小于题干。
- **题干与选项是主角**，倒计时不得挤占其可读性；选项按钮触控区 ≥ 44×44 px、间距足够防误触。
- 每屏底部固定一行**「你现在该做什么」**的指引（如「请点一个答案」「已提交，等一下」）。
- 入场页须说明**计分规则**（答得越快分越高）与**退出后如何回来**（再扫一次桌上的码，分不会丢）。
- `localStorage` 读写全部 `try/catch` 并降级为内存态（iOS 无痕会抛异常），**不得白屏**。

**七态**

| 态 | 内容 |
|----|------|
| 加载 | 真实进度条 + 分阶段文案（P-10/P-11） |
| 揭晓 | **昵称揭晓动画**（P-12），大字展示 |
| 等待 | 昵称、已入场人数、「等待主持人开始」 |
| 答题 | 中央倒计时、题号 `3 / 13`、题干、可点选项 |
| 已提交 | 选项锁定并高亮所选，显示「已提交」与剩余倒计时。**倒计时结束前可改答案**（按最后一次计分）。**不得提前透露对错** |
| 结算 | 是否答对、本题得分、我的总分、我的当前名次 |
| 终局 | 最终名次、总分、答对题数 |

### 5.2 大屏

- 1920×1080 横屏，**按宴会厅最后一排（12~15 米）可读**设计字号，不是 5 米。
- **一键全屏**入口；全屏后地址栏不可见（否则 `/screen?key=` 投给全场）。
- 显示当前题库角标（`测试题库` / `正式题库`）。
- **二维码可随时召回**：答题态与结算态须能由主持人一键重新展示入口二维码，供迟到者扫描。
- 四态：待机（二维码 + 已入场人数）／答题（中央倒计时 + 题号 + 题干 + 选项 + 已作答 N/M）／
  结算（正确答案 + 作答分布 + Top 5）／终局（Top 10）。

### 5.3 主持人控制台

- **必须显示当前题的题干、选项、正确答案** —— 主持人要播报，而他侧对大屏且灯光晃眼。
- **结算态显示唱分四项**：本题已答人数、答对人数、正确答案、当前第一名（D14）。
- **时间预算**：已用时长、剩余题数、按当前速度是否会超时。
- **连接状态指示**，并能区分「本机掉线」与「服务端不可达」—— 两者处置相反。
- **每次点击必须有明确回执**，不得让主持人无法判断操作是否生效。
- 救场按钮：延长 10 秒 / 提前结算 / 跳过 / 换备用题 / 重新公布 / 暂停继续。
- **二次确认加在不可逆操作上**：`skip`（全员清零）、`back`、`finish`、`remove`。
  **可撤销的操作不要拦**。
- 宾客管理：按昵称搜索，可「发放下一个昵称」与「移除」，**不可自定义输入昵称**。
- 一键导出 CSV（UTF-8 with BOM）。
- 支持**同口令多设备同开**，任一台操作全场生效。

---

## 6. 韧性与降级

- 断线自动重连（指数退避，封顶 8 秒；**回到前台立即重连**，不等退避）。重连后服务端首推
  `snapshot`，前端无条件全量重建。
- 重连恢复后须明确告知宾客**「你的分还在」**，不能只是静默恢复。
- 崩溃重启后从 JSONL 事件日志重放，**一律停在上一题的结算态**，由主持人决定下一步。
  宾客端须解释「刚才服务出了点问题」，**不得把宾客判成超时还把答案甩在脸上**。
- 中途入场者从当前题开始参与，此前各题记 0 分，正常进入排行榜。
- **纸质题卡（含答案）** 必须打印并交给主持人 —— 「服务器挂了转口头抢答」的预案否则无法执行。
- **四级降级预案**，每级配一句可直接照念的话术：

| 故障 | 处置 |
|------|------|
| 大屏黑屏 | 刷新大屏页面自动恢复；**题目在每个人手机上，游戏可继续**，不必暂停 |
| 主持人端掉线 | 用备用设备打开同一 `/host?key=` 链接接管 |
| 服务进程挂掉 | systemd 2 秒自动拉起 + 日志重放，等 10 秒；恢复后从上一题结算态继续 |
| **服务器整体不可达** | **无技术解**。宣布转口头抢答，**记分员用纸笔计分**。记分员、纸笔、题卡须列入当天物料清单并在彩排演练 |

## 7. 验收标准

### 7.1 同步与计分

| ID | 标准 |
|----|------|
| AC-01 | 扫码打开页面，**页面无输入框**，点「进入」即获得中文昵称并进入等待页，大屏入场人数 +1 |
| AC-02 | 主持人点「下一题」后，大屏与手机显示同一题号与题干；两端渲染完成时间戳差 ≤ 1 秒 |
| AC-03 | 大屏与手机中央均显示 **20 秒**倒计时，两端读数差 ≤ 1 秒 |
| AC-04 | 提交后选项高亮并显示「已提交」；**倒计时结束前可改答案，按最后一次提交计分**；得分与 §4.2 公式一致 |
| AC-05 | 倒计时归零后提交的答案不计分，记为 `timeout` |
| AC-06 | 每题结束大屏显示正确答案、作答分布与 Top 5；13 题结束显示 Top 10 |
| AC-07 | 关闭手机浏览器再打开，昵称与累计得分完整恢复，排名不变，且**明确提示「你的分还在」** |
| AC-08 | 第 5 题时新入场的宾客能参与第 6 题，前 5 题记 0 分且出现在排行榜中 |
| AC-09 | kill 服务端进程并重启后，所有宾客与得分完整恢复，落在上一题结算态；**宾客端有解释文案，不得被判成超时** |
| AC-10 | 100 及 400 个模拟客户端并发连接并在同一题内全部提交，不丢答案、不错分，广播延迟 < 1 秒 |
| AC-11 | `/screen` 与 `/host` 在口令错误时无法进入 |
| AC-12 | 六种主持人操作（延长 / **提前结算** / 跳过 / **换备用题** / 重新公布 / 暂停继续）计分结果与 §4.4 表格一致；**重新公布前后任何人分数不变** |
| AC-13 | 两台设备同时打开主持人端，任一台操作对全场生效，另一台状态同步；**同时点「下一题」只前进一题**（X-5） |
| AC-14 | 答题中把服务器系统时间人为前后各跳 30 秒，倒计时不错乱 |
| AC-45 | **延长 10 秒后提交的答案一律 0 分**，不存在「答得越慢分越高」 |
| AC-46 | 部分宾客掉线/息屏时，**自动结算不被提前触发**（分母是已入场人数，非在线人数，X-1） |
| AC-47 | 弱网下页面显示「已提交」时，服务端**必定已收到**该答案（X-2 回执机制） |
| AC-48 | `host:extend` 重播后，已提交宾客的**「已提交」态与所选选项不丢失**（X-3） |
| AC-49 | `extend`/`skip`/`back`/`pause`/`resume` 五个动作，宾客手机上**均出现一句可读的说明**（X-4） |

### 7.2 题库

| ID | 标准 |
|----|------|
| AC-15 | 不设任何环境变量直接启动，加载**测试题库**，大屏角标显示「测试题库」 |
| AC-16 | `QUIZ_BANK=wedding` 且文件存在时加载婚礼题库，角标显示「正式题库」；**文件缺失时进程启动失败并报明确错误，不回退** |
| AC-17 | 题库故意改错（**正题 ≠ 13 / 备用题 = 0** / 正确下标越界 / 题干为空）时，服务端启动即失败并指出具体问题 |
| AC-18 | `git status` 与远端仓库文件树中**均不出现 `data/`**（宾客数据，见 C7）。题库两份都应在库中 |
| AC-50 | 客户端流量中**不含题库与正确答案**，也不含昵称池（X-6） |

### 7.3 昵称

| ID | 标准 |
|----|------|
| AC-19 | 校验脚本对 `nicknames.json` 执行 **U1~U6 机器校验全部通过**（U7 具象性由 AC-23 人工承接） |
| AC-20 | 塞入一条违规数据（同尾词 / 超长 / 含禁用字），脚本**必须报错并指出具体条目**，且服务端启动失败 |
| AC-21 | 400 条已由用户逐条过目确认 |
| AC-22 | 400 条已由**一位 50 岁以上的人独立过目**（无人从旁解释），其指出的条目已替换 |
| AC-23 | U7 抽查：任取 30 个核心词，均可描述为「一个看得见的东西」 |
| AC-24 | 模拟 400 位宾客依次入场，昵称与池文件第 1~400 条**逐一对应、顺序一致**、全部互不重复 |
| AC-25 | 相同入场顺序重跑两次，分配结果**完全一致** |
| AC-26 | 第 401 位宾客入场触发兜底命名并告警，不崩溃 |
| AC-27 | **听辨验收**：任取 10 对昵称，在**背景噪音环境下隔 5 米朗读**，听者能正确区分 |
| AC-28 | **远距离验收**：大屏 Top 10 投到实际尺寸，**从宴会厅最后一排（12~15 米）**能逐条读出 |
| AC-29 | 手机端各状态下顶部均常驻显示昵称，**不被截断，字号不小于题干** |

### 7.4 界面与交互

| ID | 标准 |
|----|------|
| AC-31 | 手机端首屏为**单个 HTML**（CSS/JS/favicon 全内联），gzip **< 10 KB**，整页只产生 **1 个 HTTP 请求** |
| AC-40 | 模拟 400 并发拉首屏，**1 秒内**各端可见「正在进入」（P-3 流式渲染），12 秒内全部可交互 |
| AC-42 | 加载期显示**真实进度**，不得出现停滞在某一百分比的假进度（P-10） |
| AC-43 | 昵称揭晓有动画，且宾客在揭晓后**能复述自己的昵称**（P-12） |
| AC-44 | 加载与揭晓动画**不引入任何额外 HTTP 请求**，只使用 `transform`/`opacity`（P-13） |
| AC-51 | 入场页说明了**计分规则**与**退出后如何回来**两件事 |
| AC-52 | iOS 无痕模式打开不白屏，可完成全流程（允许刷新后丢身份） |
| AC-53 | 手机端每屏底部均有一行「你现在该做什么」的指引 |
| AC-35 | 大屏页面可一键进入全屏，全屏后地址栏不可见 |
| AC-54 | 答题态与结算态下，主持人可**一键把入口二维码重新打回大屏** |
| AC-38 | 主持人控制台在结算态显示**本题已答人数 / 答对人数 / 正确答案 / 当前第一名** |
| AC-55 | 主持人控制台显示**当前题的题干、选项、正确答案** |
| AC-56 | 主持人控制台显示**连接状态**，且能区分「本机掉线」与「服务端不可达」 |
| AC-57 | 主持人控制台显示**时间预算**（已用时长 / 剩余题数 / 是否会超时） |
| AC-58 | 二次确认只加在 `skip`/`back`/`finish`/`remove` 上；其余操作**不额外拦截** |

### 7.5 工程与现场

| ID | 标准 |
|----|------|
| AC-30 | `npm install` 后 `node_modules` 顶层只有 `ws`；无构建步骤，`node server.js` 直接可跑 |
| AC-32 | 主持人可一键导出 CSV，Excel 打开中文不乱码，行数 = 宾客数 × 13 |
| AC-33 | 真实微信中扫码可完成全流程（允许出现「非微信官方网页」提示条） |
| AC-34 | 移动 / 联通 / 电信**各用真实手机流量**扫码，三网均可打开 |
| AC-37 | **在婚礼场地的 WiFi 上实测可打开**；不通就得换端口重新部署并重印二维码，须在 9/28 前测出 |
| AC-39 | 主持人喊完扫码口令后，手册提供**约 12 秒的垫场话术** |
| AC-41 | systemd `LimitNOFILE` ≥ 65535；400 并发连接下无 `EMFILE` |
| AC-59 | **纸质题卡（含答案）已打印并交付主持人** |
| AC-60 | 当天物料清单含**记分员人选、纸、笔**；四级降级预案每级都有可照念的话术 |
| AC-36 | 完整彩排一次（多台真机 + 真大屏 + 真主持人），主持人**独立**跑通全流程、中途不提问 |

---

## 8. 明确不做

- 微信 OAuth 登录、获取微信头像昵称
- 宾客聊天、弹幕、红包、抽奖转盘
- 图片题、音频题、视频题
- 题库在线编辑后台；运行时热切换题库（切换需重启）
- 多场婚礼 / 多房间 / 多租户
- 用户账号体系、后台管理系统
- 国际化（仅中文）
- HTTPS、域名、ICP 备案
- 历史场次数据统计与分析
- 前端构建流水线、TypeScript、组件库、CSS 框架
- 容器化、反向代理、进程管理器
- 宾客自定义昵称；机器造词；宾客换名
- 按昵称推断真实身份的映射表；按桌号分组
- 落后宾客补答错过的题
- 宾客端「不想上大屏」开关
- 终局逐题回看自己的作答

---

## 9. 已接受的风险

| 风险 | 决策 |
|------|------|
| 单点故障：全场依赖一台服务器一个进程 | 接受。降级为口头抢答（§6） |
| 宾客清缓存 / 换设备后身份与分数不可找回 | 接受。不做找回 |
| 大屏 URL 含口令，全屏失败时会投影给全场 | 接受。`/screen` 只读，危害有限 |
| 宾客开多个无痕窗口可注册多个身份刷分 | 接受。不做设备指纹 |
| 同桌之间互相看屏幕抄答案 | 接受 |
| 现场人数超过 400 触发兜底命名 | 接受。已确认不超过 400 人 |
| **敬酒/上菜导致整桌错过数题、全桌 0 分** | 接受（不做补答）。缓解手段是**主持人用 `host:pause` 暂停**，写入操作手册 |
| **每题向垫底宾客显示具体名次**（可能令其难堪） | 接受。照常显示 |
| **上大屏被一律视为奖励，宾客无退出途径** | 接受。不做开关 |
| **发奖靠念昵称 + 本人举手认领**，嘈杂环境可能认错或无人应 | 接受（不加桌号）。缓解：U6 同音约束 + AC-27 听辨验收 + 念到谁时其手机全屏提示 |
| **首屏峰值全场等约 12 秒** | 接受。靠 P-1~P-13 压到 12 秒并保证 1 秒内有反馈 |

**总定调**：**计分若在现场失效，游戏降级为纯粹的气氛活动** —— 主持人照常播报、
大屏照常出题，不追求分数正确性，不中断婚礼流程。
因此韧性做到「不卡死」即可，不必为「绝对不丢分」过度设计；
但「打不开」与「卡死」没有降级空间，相关要求不可削减。

---

## 10. 外部依赖

| 事项 | 时限 |
|------|------|
| 婚礼题库 **16 道**（13 正题 + 3 备用） | **2026-10-01 前** |
| 一位 50 岁以上的昵称审核人（AC-22） | 彩排前 |
| 纸质题卡打印、记分员人选、纸笔（AC-59/AC-60） | 彩排前 |
| 婚礼场地 WiFi 实测（AC-37） | **9/28 前** |

服务器（`119.29.186.63`，8888 已放行）与 GitHub 仓库均已就位。
